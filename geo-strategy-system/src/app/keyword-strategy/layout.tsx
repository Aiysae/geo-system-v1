import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "关键词策略 — GEO 策略生成工具",
  description: "生成式引擎优化（GEO）策略方案生成工具",
}

export default function KeywordStrategyLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
